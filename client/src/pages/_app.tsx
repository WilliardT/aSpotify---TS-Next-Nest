import React, {Component, FC} from 'react';
import { AppProps } from 'next/app';
import { wrapper } from 'store';


const WrapperApp: FC<AppProps> = ({Component
  <Component />
})

export default wrapper.withRedux(WrapperApp);